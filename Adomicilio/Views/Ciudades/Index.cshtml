@model IEnumerable<Adomicilio.Models.Ciudad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ciudad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdEstado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ciudad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdEstado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CiudadId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CiudadId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CiudadId })
        </td>
    </tr>
}

</table>
