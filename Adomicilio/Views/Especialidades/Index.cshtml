@model IEnumerable<Adomicilio.Models.Especialidad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EspecialidadDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EspecialidadDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEspecialidad }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEspecialidad }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEspecialidad })
        </td>
    </tr>
}

</table>
